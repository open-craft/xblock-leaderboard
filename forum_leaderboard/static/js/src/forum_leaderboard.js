/* Javascript for ForumLeaderboardXBlock. */
function ForumLeaderboardXBlock(runtime, element) {
    // At the moment, this does nothing.
    $(function ($) {
        /* Here's where you'd do things on page load. */
    });
}
